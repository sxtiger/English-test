# 英语短语测试系统 — 改动与部署说明

> 更新日期:2026-08-23
> 本次改动目标:修复"容器启动后局域网无法浏览网页"的问题,并让系统在内网中同时支持 HTTP 和 HTTPS 访问。

---

## 一、问题背景与根本原因

### 故障现象

容器启动后,局域网内设备无法浏览测试系统网页;HTTPS 访问完全不可用。

### 根本原因(经实际探测确认)

| # | 原因 | 说明 |
|---|------|------|
| 1 | 每次启动容器都要联网安装依赖 | 原方案使用裸 `python:3.9-slim` 镜像,容器启动时执行 7~8 条 `pip install`。需 2~5 分钟,期间网页打不开;一旦连不上 PyPI(国内网络常见),`&&` 链中断,Flask 永远不启动,容器反复重启 |
| 2 | HTTPS 从未真正生效 | `docker-compose-https.yml` 未被成功运行过(无 `ssl/` 目录、51000 端口无监听);51001 上只有纯 HTTP,用 `https://` 访问必然失败(现代浏览器多为 HTTPS 优先) |
| 3 | 健康检查永远失败 | 原 Dockerfile 的健康检查使用 `curl`,但 slim 镜像未安装 curl |
| 4 | TLS 兼容性差 | 原代码 `ssl.PROTOCOL_TLSv1_2` 只允许 TLS 1.2 |
| 5 | 会话缺陷 | `SESSION_COOKIE_SECURE=True` 导致 HTTP 会话失效;密钥每次重启随机生成,重启后所有会话丢失 |

---

## 二、本次改动明细

### 1. `dockerfile`(重写)

- **所有 Python 依赖在构建镜像时预装** → 容器启动不再依赖网络,秒级启动,离线也可运行
- 安装 `openssl`(用于首次启动生成自签名证书)
- 健康检查改用 Python 内置库探测 `http://127.0.0.1:51000/health`(不再依赖 curl)
- 启动脚本安装到 `/usr/local/bin/docker-entrypoint.sh`(不会被 `.:/app` 卷挂载覆盖)
- 移除非 root 用户设置(避免无权限写入 NAS 挂载卷)
- 暴露端口:`51000`(HTTP)、`51001`(HTTPS)

### 2. `docker-entrypoint.sh`(新增)

- 容器启动时自动创建运行所需目录(`flask_sessions`、`uploads`、`data`、`ssl`)
- 首次启动自动生成**自签名证书(有效期 10 年)**,证书的 SAN 包含:
  - `DNS:localhost`
  - `IP:<NAS_IP>`(NAS 地址,由环境变量 `SERVER_IP` 控制)
  - `IP:127.0.0.1`
- 若需重新生成证书(如 NAS 换 IP):删除 `ssl/` 目录后重启容器

### 3. `exam_webui_https.py`(修改,现为唯一启动入口)

- **同一进程同时提供两种协议的真实服务**(不再是重定向):
  - HTTP:`0.0.0.0:51000`
  - HTTPS:`0.0.0.0:51001`
- 移除 `force_https` 强制跳转中间件
- `SESSION_COOKIE_SECURE` 改为 `False`,会话可在 HTTP/HTTPS 之间通用(内网环境)
- TLS 上下文改为 `ssl.PROTOCOL_TLS_SERVER`(支持 TLS 1.2/1.3,兼容各类浏览器)
- 密钥使用固定默认值(可用环境变量 `SECRET_KEY` 覆盖),容器重启不再丢失会话机制
- 证书加载失败时输出明确错误并退出;启动日志打印两个访问地址

### 4. `docker-compose.yml`(重写)

- 改为 `build` 方式(构建时使用项目内的 `dockerfile`),镜像名 `english-phrase-test:latest`
- 端口映射:`51000:51000`(HTTP)、`51001:51001`(HTTPS)
- 环境变量:`SERVER_IP=<NAS_IP>`、`HTTP_PORT=51000`、`HTTPS_PORT=51001`

### 5. 未改动 / 废弃的文件

| 文件 | 状态 |
|------|------|
| `exam_webui.py` | 保留未改(仅 50001 端口的 HTTP 版),新部署不再使用 |
| `docker-compose-https.yml` | **已废弃,请勿再使用,可删除**(其"启动时装依赖 + 重定向"方案已被取代) |
| `md2excel.py`、`templates/` | 无改动 |

---

## 三、改动后的验证结果(本机 Docker 实测)

| 验证项 | 结果 |
|--------|------|
| 镜像构建 | ✅ 成功 |
| `http://...:51000/` 首页 | ✅ 200 |
| `https://...:51001/` 首页 | ✅ 200(内容与 HTTP 一致) |
| 双协议 `/health` | ✅ 均返回 `{"status":"healthy"}` |
| 同一会话先 HTTP 后 HTTPS 答题 | ✅ 均 200,会话通用 |
| 证书 SAN | ✅ 包含 `IP:<NAS_IP>` |
| TLS 协商 | ✅ TLS 1.3 |
| 容器重启耗时 | ✅ 约 3.4 秒恢复访问(原方案需数分钟装依赖) |
| 容器状态 | ✅ `healthy` |

---

## 四、NAS 部署步骤

> 旧容器与新容器同名(`english-phrase-test`),部署前必须先停止并删除旧容器。

### 方式一:Container Manager 图形界面(推荐,DSM 7.2)

1. 打开 **Container Manager → 容器**,找到 `english-phrase-test`,**停止**并**删除**
2. 打开 **项目 → 新增**:
   - 项目名称:随意(如 `english-test`)
   - 路径:选择 `docker/english-test` 共享文件夹
   - 系统会自动读取其中的 `docker-compose.yml`,构建镜像并启动容器
3. 等待构建完成(首次构建约几分钟,之后重启均为秒级)

### 方式二:SSH 命令行

```bash
cd /volume1/docker/english-test

# 停止并移除旧容器
docker-compose down

# 构建新镜像(预装依赖)
docker build -f dockerfile -t english-phrase-test:latest .

# 启动
docker-compose up -d

# 查看状态与日志
docker ps
docker logs -f english-phrase-test
```

---

## 五、访问地址

| 协议 | 地址 |
|------|------|
| HTTP | `http://<NAS_IP>:51000` |
| HTTPS | `https://<NAS_IP>:51001` |

HTTPS 使用自签名证书,浏览器首次访问会提示"不安全",点击"高级 → 继续访问"即可;证书内已包含正确的 IP(SAN),可长期信任使用。

---

## 六、注意事项与常见问题

1. **DSM 防火墙**:若已启用防火墙(控制面板 → 安全性 → 防火墙),需放行 TCP **51000** 和 **51001** 端口,否则局域网设备无法连接。
2. **NAS 更换 IP 后**:
   - 修改 `docker-compose.yml` 中的 `SERVER_IP` 为新 IP
   - 删除项目目录下的 `ssl/` 文件夹
   - 重建/重启容器,证书将自动按新 IP 重新生成
3. **构建需要基础镜像**:构建时会用到 `python:3.9-slim`。NAS 上已有该镜像(原容器就在用),会直接使用本地缓存,无需联网拉取。
4. **数据位置**:短语库 `English Phrase.md`、错题库 `Error_Phrases.xlsx`、会话文件等均保存在项目目录(通过 `.:/app` 卷挂载),重建容器不会丢失数据。
5. **自定义密钥(可选)**:可在 `docker-compose.yml` 的 `environment` 中添加 `SECRET_KEY=一串随机字符串`,不设置则使用内置默认值。
6. **如何确认部署成功**:容器状态显示 `healthy`,且日志中出现:
   ```
   === 启动测试系统（HTTP + HTTPS 双协议）===
   HTTP  访问地址: http://<NAS_IP>:51000
   HTTPS 访问地址: https://<NAS_IP>:51001
   ```
